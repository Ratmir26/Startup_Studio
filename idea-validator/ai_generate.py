import sys
import json
import random

sys.stdout.reconfigure(encoding='utf-8')

idea = sys.argv[1] if len(sys.argv) > 1 else sys.stdin.read().strip()

# Большая база конкретных аргументов
pro_templates = [
    f"Решает реальную проблему: {idea}",
    "Экономит время пользователей",
    "Можно быстро запустить MVP",
    "Низкий порог входа на рынок",
    "Большой потенциал масштабирования",
    "Можно монетизировать через подписку",
    "Привлекает целевую аудиторию",
    "Не требует больших вложений на старте",
    "Тренд набирает обороты в этой нише",
    "У конкурентов пока нет такого решения",
    "Люди готовы платить за удобство",
    "Технологии уже доступны — нужно только собрать",
    "Помогает автоматизировать рутинные процессы",
    "Улучшает качество жизни пользователей",
    "Можно запустить в одиночку"
]

con_templates = [
    "Неясен реальный спрос на рынке",
    "Высокая конкуренция в этой нише",
    "Сложно привлечь первых клиентов",
    "Требуются начальные вложения",
    "Долгий путь до окупаемости",
    "Возможны юридические сложности",
    "Нужна сильная команда для запуска",
    "Пользователи могут не понять ценность",
    "Зависит от внешних факторов",
    "Сезонность может повлиять на спрос",
    "Сложно удержать пользователей",
    "Требуется постоянная поддержка",
    "Риск быстрого копирования конкурентами",
    "Маркетинговый бюджет может быть высоким",
    "Технические риски при масштабировании"
]

# Выбираем случайные 3-4 аргумента
pro_count = random.randint(3, 4)
con_count = random.randint(3, 4)

selected_pro = random.sample(pro_templates, min(pro_count, len(pro_templates)))
selected_con = random.sample(con_templates, min(con_count, len(con_templates)))

result = {"pro": selected_pro, "con": selected_con}
print(json.dumps(result, ensure_ascii=False))